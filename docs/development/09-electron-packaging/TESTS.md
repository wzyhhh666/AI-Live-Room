# 模块 09 — Electron 打包分发 测试文档

> **前置条件**: 模块 08 全部测试通过

### T9.1 — 主进程文件语法正确

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | `npx tsc electron/main.ts --noEmit --skipLibCheck` | 无语法错误 |

### T9.2 — 安装依赖成功

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | `npm install` | electron 相关包安装成功 |

### T9.3 — 打包配置正确

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | `npx electron-builder --win --dir` | 在 `release/` 下生成 `win-unpacked/` |

### T9.4 — 应用图标存在

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 检查 `resources/icon.ico` | 文件存在 |

### T9.5 — 构建无错误

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | `npx vite build` | 成功 |
| 2 | `npx electron-builder --win --dir` | 成功 |

**通过标准**: 全部 5 个测试通过。
