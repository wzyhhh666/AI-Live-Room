# 模块 06 — 后端敏感词过滤 测试文档

### T6.1 — 正常内容通过

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 发送 "你好世界" | 正常发送 |

### T6.2 — 拦截模式

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 发送包含 BLOCK 级别敏感词的消息 | error `'Content blocked'` |
| 2 | 查询 MySQL danmaku_messages | 未写入该条记录 |

### T6.3 — 替换模式

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 发送包含 REPLACE 级别词的消息 | 消息正常发送，但敏感词被替换为 `***` |

### T6.4 — 启动无报错

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | `node src/server.js` | 无 `❌` 错误 |

**通过标准**: 全部 4 个测试通过。
